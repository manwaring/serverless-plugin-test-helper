service:
  name: test-helper-default-js

plugins:
  - serverless-plugin-test-helper

provider:
  name: aws
  stage: example
  runtime: nodejs10.x

functions:
  hello:
    handler: handler.hello
    events:
      - http:
          method: get
          path: hello
